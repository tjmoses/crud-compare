/**
 * @module crud-object-diff
 */

import { compareObjectVals, compareArrayVals } from './main';
import { isEqualObject, isEqualArray } from './helpers';

export { isEqualObject, isEqualArray, compareArrayVals, compareObjectVals };
